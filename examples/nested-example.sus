card nested {

    into ~

    {
        outof bash

        bashrc          -> .bashrc
        bash_aliases    -> .bash_aliases
        bash_login      -> .bash_login
    }

    {
        outof zsh
        
        zshrc  -> .zshrc
        zlogin -> .zlogin
    }

}
