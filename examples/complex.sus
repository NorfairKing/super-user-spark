card main {

    spark card configs
    spark card scripts

}

card configs {

    into ~

    spark file bash.sus bash

    {
        outof zsh

        zshrc           -> .zshrc
        zshenv          -> .zshenv
        zlogin          -> .zlogin
        
    }

    vim -> .vim

}

card scripts {

    outof scripts
    {
        into /usr
        {
            into bin
            kind copy

            clean -> clean
        }
    }

}
